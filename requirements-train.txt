# Full install for Colab / train_grpo.py (includes base API deps)
-r requirements.txt

openenv

# Testing
pytest>=7.4.0

# Machine Learning & LLM (Colab GPU)
numpy>=1.24.0
matplotlib>=3.7.0
# unsloth_zoo (Colab) requires torch<2.11. Newer torch breaks resolution; mergekit may skip C++ ext (harmless).
torch>=2.4.0,<2.11.0
huggingface_hub>=0.22.0
# Aligned with unsloth[colab-new] (>=4.51.3). Do not cap <4.46: that conflicts with unsloth.
# train_grpo.py shims TRANSFORMERS_CACHE for llm_blender (trl) on new transformers.
transformers>=4.51.3,<=5.5.0
# unsloth_zoo expects trl in this band (see pip install unsloth).
trl>=0.18.2,<=0.24.0,!=0.19.0
# trl TRL/GRPO imports mergekit (mergekit_utils) — not always pulled as a transitive dep
mergekit>=0.0.6

# Unsloth (git required; available on Colab, not in slim Docker)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
